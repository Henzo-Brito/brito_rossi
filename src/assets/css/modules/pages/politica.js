import { styled } from "styled-components";

export const SPolitica = styled.section`
	padding: clamp(15px, 2dvw, 20px);
	h1 {
		font-size: clamp(20px, 4dvw, 45px);
	}
	p {
		margin-top: 10px;
		font-size: clamp(14px, 3dvw, 20px);
		text-align: justify;
	}
`;
